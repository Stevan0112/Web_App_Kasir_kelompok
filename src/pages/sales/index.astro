---
import Dashboard from "../../layouts/Dashboard.astro";

const resp = await fetch('https://backend-webappkasir-production.up.railway.app/api/penjualan');
const sales = await resp.json();
---

<Dashboard title="Sales">
    <div class="space-y-6">
        <!-- Card -->
        <section class="bg-white rounded-xl shadow-sm overflow-x-auto">
            <!-- Card Header -->
            <div class="flex items-center justify-between px-4 py-3 border-b">
                <h2 class="font-bold text-2xl">Top Sales Weekly</h2>
                <div class="flex gap-2">
                    <!-- <button class="px-3 py-1 text-sm border rounded-md hover:bg-gray-100">
                        Filter
                    </button> -->
                    <button class="px-3 py-1 text-sm border rounded-md bg-gray-100 hover:bg-gray-200">
                        See All
                    </button>
                    <button class="px-3 py-1 text-sm bg-pink-500 text-white rounded-md hover:bg-pink-600">
                        + Add
                    </button>
                </div>
            </div>

            <!-- Table -->
            <table class="w-full text-sm">
                <thead class="text-gray-500 border-b">
                    <tr>
                        <th class="text-left px-2 py-3">Order ID</th>
                        <th class="text-left py-3">Transaction Date</th>
                        <th class="text-left py-3">Total Amount</th>
                        <th class="text-left py-3">Customer Name</th>
                        <th class="text-center py-3">Action</th>
                    </tr>
                </thead>
                <tbody>
                {sales.map((order: any) => (
                    <tr class="border-b hover:bg-gray-100">
                        <!-- Product -->
                        <td class="py-3 px-2 text-left font-medium">{order.id}</td>
                        <td class="py-3">{order.transaction_date}</td>
                        <td class="py-3">{order.total_amount}</td>
                        <td class="py-3">{order.customer_name}</td>

                        <!-- Action -->
                        <td class="py-3 text-center">
                            <div class="flex justify-center gap-2">
                                <button class="p-1 rounded-full bg-gray-200 hover:bg-gray-300">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon icon-tabler icons-tabler-outline icon-tabler-pencil"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M4 20h4l10.5 -10.5a2.828 2.828 0 1 0 -4 -4l-10.5 10.5v4" /><path d="M13.5 6.5l4 4" /></svg>
                                </button>
                                <button class="p-1 rounded-full bg-gray-200 hover:bg-gray-300">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="currentColor" class="icon icon-tabler icons-tabler-filled icon-tabler-trash"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M20 6a1 1 0 0 1 .117 1.993l-.117 .007h-.081l-.919 11a3 3 0 0 1 -2.824 2.995l-.176 .005h-8c-1.598 0 -2.904 -1.249 -2.992 -2.75l-.005 -.167l-.923 -11.083h-.08a1 1 0 0 1 -.117 -1.993l.117 -.007h16z" /><path d="M14 2a2 2 0 0 1 2 2a1 1 0 0 1 -1.993 .117l-.007 -.117h-4l-.007 .117a1 1 0 0 1 -1.993 -.117a2 2 0 0 1 1.85 -1.995l.15 -.005h4z" /></svg>
                                </button>
                            </div>
                        </td>
                    </tr>
                ))}
                </tbody>
            </table>
        </section>

        <!-- Pagination -->
        <div class="flex justify-center gap-2">
            <!-- Prev Button -->
            <!-- {page > 1 && (
                <a href={`?page=${page - 1}`} class="px-3 py-1 bg-gray-300 hover:bg-gray-400">
                    ←
                </a>
            )} -->

            <!-- Page Numbers -->
            <!-- {Array.from({ length: totalPages}).map((_, i) => {
                const pageNumber = i + 1;
                return (
                    <a href={`?page=${pageNumber}`} class={`w-8 h-8 flex items-center justify-center bg-gray-300 ${pageNumber === page ? "bg-gray-400" : "hover:bg-gray-400"}`}>
                        {pageNumber}
                    </a>
                );
            })} -->

            <!-- Next Button -->
            <!-- {page < totalPages && (
                <a href={`?page=${page + 1}`} class="px-3 py-1 bg-gray-300 hover:bg-gray-400">
                    →
                </a>
            )} -->

            <!-- Old Pagination -->
            <!-- <button class="w-8 h-8 bg-gray-300 hover:bg-gray-400">1</button>
            <button class="w-8 h-8 bg-gray-300 hover:bg-gray-400">2</button>
            <button class="w-8 h-8 bg-gray-300 hover:bg-gray-400">→</button> -->
        </div>
    </div>
</Dashboard>